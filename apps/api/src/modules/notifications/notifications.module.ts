import { Module } from "@nestjs/common";

/** Phase 2: Prisma models (Notification, NotificationPreference) exist; delivery (in-app bell, email) not yet built. */
@Module({})
export class NotificationsModule {}
