import { Module } from "@nestjs/common";

/** Phase 2: Prisma models (LearningPath, LearningPathStep, Prerequisite) exist; auto-unlock engine not yet built. */
@Module({})
export class LearningPathsModule {}
