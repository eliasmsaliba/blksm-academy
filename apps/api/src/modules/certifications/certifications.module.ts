import { Module } from "@nestjs/common";

/** Phase 2: Prisma models (Certification, CertificateTemplate, IssuedCertificate) exist; PDF/QR generation not yet built. */
@Module({})
export class CertificationsModule {}
